package counter

/*
func CounterHeader(HTTPHeaders []logparser.HTTPHeader) {
	headerRules := rules.HeaderRules()


	for _, header := range HTTPHeaders {
		if threshold, ok := headerRules[header.Name]; ok {
			if headerCount >= threshold {
				fmt.Println("Number of requests is >= threshold")
			}


		}
	}
}
*/
